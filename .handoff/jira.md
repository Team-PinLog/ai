# Jira 인계 — AI 파트 (S15P11A705)

> 다른 기기/세션에서 이어받기 위한 인계 문서. **환경 정보(§3)만 있으면 재조회 없이 바로 Jira 조작 가능.**
> 최종 갱신: 2026-07-24

## 1. 에픽·티켓 현황

```
에픽 S15P11A705-32  [AI] AI 파이프라인 구축 — 진행 중 (담당 이정헌)
  ├─ 완료 8:  -21(FastAPI 세팅) -29(컨벤션 이식) -30(M5 정합화) -31(T9 보존원칙)
  │           -34(공용 설계) -35(스키마·평가) -36(FastAPI 구현) -37(문서·컨벤션)
  └─ 진행 중 1: -33 (E3 통합 테스트·Dockerfile)
```

- 산하 전부 담당 = 이정헌. 소급 4건(-34~-37)은 설명 첫 줄에 `retrospective` 명시.
- **에픽 상태**: 산하 1건(-33) 진행 중이라 에픽도 "진행 중". `-33` 완료되면 에픽을 "완료"로.

## 2. 대기 작업 (인계 대상)

- **`-33` (E3)** 완료 대기: E3 세션이 **PR2 머지**를 회신하면
  1. `transitionJiraIssue(S15P11A705-33, {id:"31"})` → 완료
  2. 이어서 **에픽 `-32` 완료 검토** — 산하가 전부 완료면 `transitionJiraIssue(S15P11A705-32, {id:"31"})`.
- E3 현황(2026-07-24): PR#17(구현 문서화)은 병합됐으나 **PR2 미착수** → `-33` 진행 중 유지가 맞음.
- 타 파트 `-26`·`-19`·`-17`(GitHub-Jira 연동)은 **무개입**. 연동 완성 전까지 상태 전환은 수동.

## 3. ★ Jira 환경 정보 (재조회 불필요) ★

| 항목 | 값 |
|---|---|
| MCP server | `c7e2347d-e4ac-4067-915a-ff6eada6a0f3` (Atlassian) |
| 사이트 | `ssafy.atlassian.net` |
| **cloudId** | `15a21def-11f5-4358-ab48-074194685963` |
| 프로젝트 키 | `S15P11A705` (이름 "A705 - PinLog", project id `21609`) |
| 프로젝트 타입 | company-managed (`simplified:false`) |
| **본인 accountId** (이정헌 = colosair@gmail.com) | `712020:d8767954-0faa-4dc2-a50e-3ab7ce396a86` |

**이슈 타입 id** (createJiraIssue `issueTypeName` 또는 id):
| 타입 | id |
|---|---|
| 에픽 | `10000` |
| 스토리 | `10001` |
| 작업 | `10002` |
| 하위 작업 | `10003` |
| 버그 | `10004` |

**전환 id** (`transitionJiraIssue` `transition.id`) — 프로젝트 공통 워크플로:
| → 상태 | transition id | (status id) |
|---|---|---|
| 해야 할 일 | `11` | 10000 |
| **진행 중** | `21` | 3 |
| **완료** | `31` | 10001 |

**에픽 귀속**: company-managed지만 `parent`로 작동(검증 완료).
- 발급 시: `createJiraIssue(..., parent:"S15P11A705-32")`
- 기존 티켓: `editJiraIssue(key, fields:{"parent":{"key":"S15P11A705-32"}})`

**담당자 배정**: `createJiraIssue(..., assignee_account_id:"712020:d8767954-0faa-4dc2-a50e-3ab7ce396a86")` 또는 `editJiraIssue(key, fields:{"assignee":{"accountId":"712020:d8767954-0faa-4dc2-a50e-3ab7ce396a86"}})`.

## 4. 예정 발급 티켓

- **[AI] 문서 정합화** — 전수 조사 갭 반영. `parent = S15P11A705-32`, 담당 이정헌, 발급 즉시 "진행 중"(id 21).
  - **문서화 세션이 착수 시 요청 예정**. 요청 오면 발급 → 번호 회신.

## 5. 컨벤션 메모 (요약; 원본 = 메모리 `pinlog-git-convention`)

- **티켓 라이프사이클**: 작업 착수 시 발급 + 본인 배정 + "진행 중" → 머지 후 "완료" 수동 전환. GitHub-Jira 연동(`-19`) 완성 전까지 수동.
- **발급 기준**: 의미 있는 작업 단위(기능·설정·문서 세트) 1건당 티켓 1개. 오타·링크 등 초소형 수정은 기존 티켓에 묶거나 chore로 합쳐 남발 방지.
- **연동 후 자동/수동 분리**: 자동 — PR 생성→진행 중, 머지→완료 (규칙 `-26`, 전제 연동 `-19` 미완). 수동 — 발급·담당자 배정·티켓 내용 작성.
- 커밋/브랜치: `{type}({jira-key}): {summary}` / `{type}/{jira-key}-{summary}`. squash 병합 + 브랜치 삭제.

## 6. 세션 연계 (참고)

- **E3 세션** "[pinlog] AI 프로젝트 세팅" (`feat/test-harness`) — `-33` 담당. PR2 머지 시 회신 예정.
- **문서화 세션** "[pinlog] AI 파트 문서화 작업" — 에픽 키 전달받아 `-31` 자체 마감. "문서 정합화" 티켓 요청 예정.
- 세션 간 전달은 `mcp__ccd_session_mgmt__send_message`.
